# EFTA Healthcare XAI Research Console

This repository extends the validated **Evaluation Before Trust Architecture (EFTA)** research implementation into a locally runnable application. It keeps the machine-learning code in `ml/`, exposes it through a modular FastAPI service, and provides a React/Vite dashboard for real-time case analysis.

> **Research use only.** The application is not medical advice, a diagnostic device, or an autonomous clinical decision system. `ABSTAIN_ESCALATE` is the safety posture whenever any required gate fails.

## Architecture

```text
React/Vite dashboard
        ↓ /api (Vite development proxy)
FastAPI REST API
        ↓
Prediction and EFTA services
        ↓
Preserved ml/ implementation: data_loader → calibrated model → G1/G2/G3
        ↓
Structured prediction, gate, explanation, and decision response
```

## Project structure

```text
EFTA/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── api/health.py
│   │   ├── api/model.py
│   │   ├── api/prediction.py
│   │   ├── schemas/prediction.py
│   │   └── services/
│   │       ├── efta_service.py
│   │       ├── model_service.py
│   │       └── prediction_service.py
│   └── requirements.txt
├── frontend/
│   ├── src/App.jsx
│   ├── src/main.jsx
│   ├── src/services/api.js
│   ├── src/styles.css
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
├── ml/
│   ├── config.py
│   ├── data_loader.py
│   ├── models.py
│   ├── gates.py
│   ├── experiments.py
│   └── smoke_test.py
└── README.md
```

## Prerequisites

- Python **3.10 or newer**; Python 3.12 is supported.
- Node.js **18 or newer** and npm.
- Internet access for the first dependency installation and the Google Fonts stylesheet used by the dashboard.

## Backend setup and start

From the repository root:

```bash
cd backend
python -m venv .venv
# macOS/Linux:
source .venv/bin/activate
# Windows PowerShell: .venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --reload
```

The API runs at **http://127.0.0.1:8000**. Swagger documentation is available at **http://127.0.0.1:8000/docs** and the OpenAPI JSON document is at `/openapi.json`.

## Frontend setup and start

In a second terminal from the repository root:

```bash
cd frontend
npm install
npm run dev
```

Open **http://localhost:5173**. Vite proxies `/api/*` calls to `http://127.0.0.1:8000`, so the browser uses the same relative API paths in development. CORS is also enabled for both localhost frontend origins in FastAPI.

## API endpoints

| Method | Path | Purpose |
|---|---|---|
| GET | `/api/health` | API and fitted-model readiness |
| GET | `/api/model` | Model, dataset, feature count, feature names, and preprocessing metadata |
| GET | `/api/gates` | Definitions and pass conditions for G1, G2, and G3 |
| GET | `/api/experiments` | Read-only access to the fixed outputs generated by `ml/experiments.py` |
| POST | `/api/predict` | Validate 30 raw features, run the real model and EFTA gates, and return a structured result |

A prediction request contains exactly 30 finite numeric values in the native scikit-learn Wisconsin Diagnostic Breast Cancer feature order:

```json
{
  "features": [17.99, 10.38, 122.8, 1001, 0.1184, 0.2776, 0.3001, 0.1471, 0.2419, 0.07871, 1.095, 0.9053, 8.589, 153.4, 0.006399, 0.04904, 0.05373, 0.01587, 0.03003, 0.006193, 25.38, 17.33, 184.6, 2019, 0.1622, 0.6656, 0.7119, 0.2654, 0.4601, 0.1189],
  "model_name": "logistic_regression"
}
```

Optional threshold fields (`confidence_threshold`, `faithfulness_drop_threshold`, and `stability_threshold`) are validated by Pydantic and otherwise default to the preserved `ml/config.py` values.

## How EFTA is invoked

`backend/app/services/model_service.py` loads the original `ml/data_loader.py`, fits the original calibrated model from `ml/models.py` using seed 40, and applies the original training-only scaler to raw HTTP features. `backend/app/services/efta_service.py` calls the original `ml/gates.py` functions for G1 local uncertainty, G2 top-k masking faithfulness, G3 noisy explanation stability, and conjunction decision logic. No prediction is hard-coded or simulated by the UI.

The dataset semantics are explicit: class 0 is malignant and class 1 is benign. The preserved EFTA G2 methodology defines its explanation target as class 1, because the original helper and SHAP fallback evaluate `predict_proba()[:, 1]`; this is preserved rather than silently redefining the research gate. The API therefore returns `malignant_probability`, `benign_probability`, and `probability` (the predicted-class probability) separately. `confidence` is always the highest class probability, so it matches `probability` for the predicted class.

The dashboard labels the 30 inputs with the actual `load_breast_cancer().feature_names` values. The “Load sample” action uses a real 30-value example-shaped vector in the dataset’s raw units; it is not a fake API response.

## Original experiment suite

The original research experiment remains under `ml/` and can be run without the web application:

```bash
cd ml
python smoke_test.py
python experiments.py
```

The full experiment writes `ml/results/raw_results.csv` and `ml/results/comparison_table.md`.

The dashboard’s **Experiment protocol** view loads those files through `GET /api/experiments`. It displays the actual two-model, ten-seed summary, protocol metadata, and a limited raw-results table. The generated batch output contains coverage, selective error, accuracy, and unsupported release rate under clean and simulated shift conditions. It does not contain dataset-level G1/G2/G3 gate counts; the UI explicitly keeps those gate results at the individual-case prediction scope.

## Validation performed

- Existing ML smoke test: passed after the existing validated files were copied unchanged into `ml/`.
- Python compilation: passed for backend modules and ML modules.
- FastAPI application import and server startup: verified with a live Uvicorn process.
- `/api/health`, `/api/model`, and `/api/gates`: verified with HTTP requests.
- `/api/predict`: verified with a real 30-feature request and real G1/G2/G3 output.
- Invalid prediction input: verified to return HTTP 422 for the wrong feature count.
- Frontend dependency install and `npm run build`: verified.
- Frontend/backend integration: verified through Vite’s `/api` proxy and the dashboard’s real fetch client.
- Probability semantics regression tests: malignant and benign predictions, confidence consistency, G1 behavior, and class-1 G2 faithfulness behavior pass.

## Limitations and assumptions

The application fits one cached Logistic Regression model with seed 40 for interactive inference. The original experiment module still evaluates both models and all ten seeds. The existing G5 subgroup check is not surfaced in the case-level UI because the requested interactive response is specifically G1/G2/G3. The first continuous feature subgroup remains an engineered proxy rather than a demographic or protected-class attribute. SHAP is optional in the original gate implementation; if it is not installed, the preserved model-agnostic fallback is used. The frontend is intentionally a research console and does not implement authentication, persistence, clinical interoperability, or production deployment controls.
