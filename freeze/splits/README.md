# Persistent Splits

The current smoke split manifests are retained at `../../ml/results/smoke/splits/`. The confirmatory run must persist one manifest per dataset and prespecified seed using the existing split protocol.
